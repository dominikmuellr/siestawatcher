name: siestawatcher
description: "Who has siesta?"
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.10.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  geolocator: ^10.1.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
